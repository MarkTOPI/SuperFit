<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.superfit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true">
        <activity android:name=".MyBody" />
        <activity android:name=".recipe.RecipesList" />
        <activity android:name=".recipe.RecipeScreen" />
        <activity android:name=".mainscreen.mainScreen" />
        <activity android:name=".authorizationcode.AuthorizationCode" />
        <activity android:name=".authorization.Authorization" />
        <activity android:name=".exercisesScreen.ExercisesScreen" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.SuperFit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>